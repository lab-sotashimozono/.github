name: CI
on:
  push:
    branches: [main]
  pull_request:

jobs:
  ci:
    uses: lab-sotashimozono/.github/.github/workflows/julia-ci.yml@main
    with:
      runner: '["self-hosted","rosina"]'
      setup_julia: false   # rosina provides Julia via juliaup
